# Core scientific computing
numpy>=1.23.0
pandas>=1.5.0
scipy>=1.9.0

# Machine learning (used for TF-IDF, data loading, metrics)
scikit-learn>=1.1.0

# Deep learning – model training on CPU / CUDA / Apple MPS
# Install command varies by platform – see README or setup_and_run.ipynb:
#   Apple Silicon (MPS) : pip install torch
#   NVIDIA GPU  (CUDA)  : pip install torch --index-url https://download.pytorch.org/whl/cu121
#   CPU only            : pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# Visualisation (matplotlib only – no seaborn)
matplotlib>=3.6.0

# Configuration
pyyaml>=6.0

# Progress bars
tqdm>=4.64.0

# Jupyter (for notebooks)
notebook>=6.5.0
ipykernel>=6.0.0
